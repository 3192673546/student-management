<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="student.mapper.StuMapper">
    <insert id="addstu" parameterType="student.pojo.Student">
        INSERT INTO student(id,name) VALUES (#{id},#{name})

    </insert>
    <update id="updstu">

        update student set id=#{id},name=#{name} where id=#{id}
    </update>
    <delete id="delstu">
        delete from student where id=#{id}
    </delete>
    <select id="querystu" resultType="student.pojo.Student">
        select * from student
    </select>
    <select id="findById" resultType="student.pojo.Student">
        select * from student where id=#{id}
    </select>
</mapper>